import { Envelope } from 'react-bootstrap-icons'
import { IconContainer } from './IconContainer'

export const EmailLink = () => {
	return (
		<IconContainer url={'mailto:mansilladario94@gmail.com'}>
			<Envelope size={40} />
		</IconContainer>
	)
}
